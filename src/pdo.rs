
/*
pub enum PdoId {
    Tx(u8),
    Rx(u8)
}

impl Into<CobId> for (PdoId, u8) {
    fn into((pdo, node_id): (PdoId, u8)) -> CobId {
        match pdo {
            PdoId::Tx(pdo_id) => CobId::PdoTx{}
        }
    }
}
*/
